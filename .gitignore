node_modules/
dist/
.env
.env.*
!.env.example
apps/*/.env
apps/*/.env.*
!apps/*/.env.example
.DS_Store
*.log
coverage/
.vite/
.turbo/
*.tsbuildinfo

# Playwright MCP scratch artifacts (snapshots, console logs)
.playwright-mcp/

# Verification screenshots dropped in the repo root (throwaway artifacts).
# Scoped to root so real image assets under apps/*/ (icons, splash) stay tracked.
/*.png
